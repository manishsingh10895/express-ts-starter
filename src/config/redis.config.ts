export type RedisConfig = {
    CHANNELS: {
        //ADD Channels for pub/sub here
    },
    KEYS: {
        //ADD Keys for redis memory store here
    }
}

export const REDIS_CONFIG = {
    CHANNELS: {

    },
    KEYS: {
        
    }
}